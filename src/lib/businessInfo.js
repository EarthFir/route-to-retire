// ─── Business identity ────────────────────────────────────────────────────────
// Single source of truth for the operator identity shown on the Privacy and
// Terms pages. Fill these in once the trading name / entity is settled — the
// pages that reference this file render each detail only when it's non-empty,
// so leaving a field blank simply omits that sentence rather than showing a
// placeholder to visitors.

export const BUSINESS_INFO = {
  // e.g. "Route to Retire" or a separate registered trading name.
  tradingName: "",
  // e.g. "a sole trader" or "Route to Retire Ltd, registered in England and
  // Wales (company no. 12345678)".
  legalStatus: "",
  // Correspondence address, e.g. an accountant's registered office address.
  address: "",
  // Contact email, e.g. an @routetoretire.co.uk mailbox.
  contactEmail: "",
};
